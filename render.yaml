services:
  - type: web
    name: ai-cursor
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run virtual_mouse.py --server.address 0.0.0.0 --server.port $PORT --server.headless true
